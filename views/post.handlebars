<!DOCTYPE html>
<html>
<head>
  <title>{{post.title}}</title>
  <link rel="stylesheet" href="../public/styles/style.css">
</head>
<body>
  <h1>{{post.title}}</h1>
  <p>{{post.content}}</p>
  <h2>Comments</h2>
  <ul>
    {{#each comments}}
      <li>{{this.text}}</li>
    {{/each}}
  </ul>
</body>
</html>